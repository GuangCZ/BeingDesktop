- 修复篝火与围炉的消息发送，发送失败或结果未确认时保留草稿，避免重复提交。
- 优先使用已有 Heart Portal，沿用其工作区、配置与原有启动方式；进程暂时退出时不再由 Desktop 接管。
- 分开保存 Desktop 项目目录与 Portal 工作区，切换项目不再影响已有 Portal。
- 保留本机编排与 Being 原生能力的边界：本机工具桥不可用时，原生通信和对话仍可继续。

下载：macOS（Apple 芯片）选 `.dmg`；Windows 选 `.exe`，或完整解压 `.zip` 后运行。

macOS 沿用项目现有的固定本地签名，未经过 Apple 公证。
