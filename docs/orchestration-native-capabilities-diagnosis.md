# 编排边界修复与模型断流诊断（2026-09-09）

## 修复

两处客户端注入指令原先把 Being 限定为“只负责”编排，未区分本机执行与 Being 原生能力。现将代码、工作区调查、文件、命令、测试和浏览器操作委派给 Worker；原生通信、记忆、身份与自身状态管理及所需读取、HTTP 调用按用户授权直接执行。原生 HTTP 不得绕过本机执行限制，Being 凭据不得交给 Worker。

发送聊天原先等待本机桥连接并要求编排策略核验成功，桥断线会连带阻塞原生任务。现在发送时报告本机执行策略状态；启动 Worker 和展示执行结果时仍严格核验。会话、身份及模式切换检查保持有效。设置说明与文档同步了这一边界。

## 已取得的故障证据

检查的是安装版对应的源码，以及生产会话存储的离线副本；没有修改历史或重发原通知。时间均为北京时间。

| 请求 | 观察 |
| --- | --- |
| 18:15:23.253 发起，18:16:56.912 结束 | 总计 93.659 秒；186 个 `reasoning` 事件，随后一个 `error`：`LLM stream stalled: no data received for 60s`。没有收到 `tool_use` 或 `tool_result`。 |
| 18:21:35.267 发起，18:22:13.734 结束 | 原有后续请求调用原生 `http`，目标为 `POST /api/bonfire/speak`；返回 `is_error: false`，最终收到 `message_stop`。本次诊断未重新发布通知。 |

第一条请求的 ID 为 `8e1c193f-605d-42d8-9503-cf7010a57de2`，流 ID 为 `a9a35ab7-2a77-4984-8a72-c461a91fe758`，便于服务端对照日志。

错误来自已接收的服务端 SSE `error` 事件，并非 Desktop 工具拒绝或本机 Worker 超时。当前 Loom 前端的静默预算分别为首事件 75 秒、推理 90 秒、文本 45 秒、工具 300 秒；前端恢复只续读已有流，不自动重发任务。仓库与当前 Loom 页面均没有生成上述固定 60 秒错误的实现。

诊断时配置为 `openrouter` / `deepseek-v4-flash`，未使用旧 `/orchestrator/` 网关。此为当前配置读数，不能据此证明故障发生时配置完全相同。

## 实测与限制

使用修复后的生产环境指令，向当前 Being 提交一次明确限定的原生只读诊断：`GET /api/bonfire/hear?limit=1`。请求未调用 Worker，未发送通知或修改设置。HTTP 流头在 168 毫秒返回，原生工具调用在 9.533 秒出现，工具成功结果在 9.637 秒返回，21.083 秒收到正常结束事件；观测到的最大字节间隔为 11.239 秒。本次未复现断流。

因此已将故障定位到服务端模型流阶段，排除了“本次卡在 Worker 执行”以及“原生 http 被全局禁止”。尚不能区分上游提供商停顿、服务端与上游之间的连接中断或服务端流适配器问题：客户端没有当时的上游请求 ID、HTTP 日志、逐事件接收时间或服务端超时实现。不能把提示词过严直接认定为此次断流的原因，也不能宣称已修复模型服务本身。

进一步定位需对照上述流 ID 和 18:15:23–18:16:57 时间窗的服务端模型请求日志，检查最后接收字节/有效事件、提供商请求 ID、结束原因以及 60 秒看门狗的计时对象。本次没有盲目延长超时、切换共享模型或自动重发原任务。

## 验证

- 全量测试：1063 项，1062 通过，1 项跳过，0 失败。
- 语法检查通过。
- 新增用例覆盖原生请求在本机桥不可用时仍可发送、文本与多模态内容保留，以及策略未通过时 Worker 零启动。
- Electron 编排设置、逐请求环境注入测试通过。
- 当前部署 Loom 的离线流测试通过，覆盖断线续读、错误重放、去重、重载和会话隔离，确保不重发任务。
- macOS 应用打包及固定签名校验通过。
- 已更新 `/Applications/Being Desktop.app` 并正常重启。安装后的五个变更运行时文件与源码逐一一致，固定签名验证通过，仅有一个新的主进程；Desktop ID 与完整设置文件的哈希均未变化。旧应用保存在本工作区 `.local/orchestration-diagnosis/backup/`，可回滚。此次是本地修复，未发布新的 GitHub Release。
